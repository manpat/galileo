using UnityEngine;
using System.Collections;

public class ReturnShip : MonoBehaviour {

	GameObject Ship;
	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () 
	{
	
	}

	/*void OnCollisionEnter(Collision col)
	{
		if (col.gameObject.tag == "Player" && hasCollectedCompound == true) 
		{
			GameObject.Find("GodObject").GetComponent<EventManager>().OnReturnToShip();
		}
	}*/
}
